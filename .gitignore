node_modules/
dist/
data/*.json
!data/.gitkeep
# Uploaded / local movie files (keep the folder, ignore the videos)
media/*
!media/.gitkeep
.env
.env.local
*.log
.DS_Store
.vite/

# MakeEmoji offline pack (~380MB) — fetched by scripts/fetch-emoji-offline.mjs
artifacts/emoji-offline/
.tmp-dn-cards-emoji/

# TypeScript sources for the ported /emoji module (runtime uses the .js build)
src/bot/emoji/**/*.ts
src/bot/animations/**/*.ts
src/bot/search/**/*.ts
